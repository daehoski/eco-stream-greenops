apiVersion: kafka.strimzi.io/v1beta2
kind: Kafka
metadata:
  name: my-cluster
  namespace: kafka
spec:
  kafka:
    version: 3.7.0
    replicas: 1
    listeners:
      - name: plain
        port: 9092
        type: internal
        tls: false
      - name: external
        port: 9094
        type: nodeport
        tls: false
    config:
      offsets.topic.replication.factor: 1
      transaction.state.log.replication.factor: 1
      transaction.state.log.min.isr: 1
      default.replication.factor: 1
      min.insync.replicas: 1
    storage:
      type: ephemeral  # 👈 중요! 하드디스크 없이 램/임시공간 사용
  zookeeper:
    replicas: 1
    storage:
      type: ephemeral  # 👈 중요! 주키퍼도 하드디스크 없이 사용
  entityOperator:
    topicOperator: {}
    userOperator: {}
